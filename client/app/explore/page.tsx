"use client";
import {useEffect,useState} from "react";import Navbar from "@/components/navbar";import Footer from "@/components/footer";import CampaignCard from "@/components/campaign-card";import {Campaign} from "@/lib/types";import {Search,ChevronLeft,ChevronRight,RefreshCw} from "lucide-react";import {api} from "@/lib/api";
export default function Explore(){const [items,setItems]=useState<Campaign[]>([]);const [query,setQuery]=useState("");const [category,setCategory]=useState("All");const [sort,setSort]=useState("highest");const [deadline,setDeadline]=useState("");const [maxGoal,setMaxGoal]=useState("");const [page,setPage]=useState(1);const [pages,setPages]=useState(1);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const load=async()=>{setLoading(true);setError("");try{const p=new URLSearchParams({page:String(page),search:query,category,sort});if(deadline)p.set("deadline",deadline);if(maxGoal)p.set("maxGoal",maxGoal);const data=await api<{items:Campaign[];pages:number}>(`/campaigns?${p}`);setItems(data.items);setPages(data.pages)}catch(e){setError(e instanceof Error?e.message:"Could not load campaigns")}finally{setLoading(false)}};useEffect(()=>{const t=setTimeout(load,250);return()=>clearTimeout(t)},[query,category,sort,deadline,maxGoal,page]);return <><Navbar/><main className="container-app py-14"><p className="eyebrow">Discover your next cause</p><h1 className="mt-3 text-5xl font-black tracking-tight">Ideas looking for believers.</h1><div className="my-9 grid gap-3 rounded-3xl bg-white p-3 shadow-soft md:grid-cols-2 lg:grid-cols-5"><label className="flex items-center gap-3 px-3 lg:col-span-2"><Search size={19}/><input aria-label="Search campaigns" className="w-full py-3 outline-none" placeholder="Search campaigns..." value={query} onChange={e=>{setQuery(e.target.value);setPage(1)}}/></label><select aria-label="Category" className="rounded-2xl bg-cream px-4 py-3 font-bold" value={category} onChange={e=>{setCategory(e.target.value);setPage(1)}}><option>All</option>{["Technology","Community","Health","Art","Environment","Education"].map(c=><option key={c}>{c}</option>)}</select><select aria-label="Sort campaigns" className="rounded-2xl bg-cream px-4 py-3 font-bold" value={sort} onChange={e=>setSort(e.target.value)}><option value="highest">Highest funded</option><option value="newest">Newest</option><option value="deadline">Deadline</option></select><input aria-label="Maximum funding goal" className="rounded-2xl bg-cream px-4 py-3" type="number" placeholder="Max goal" value={maxGoal} onChange={e=>setMaxGoal(e.target.value)}/><input aria-label="Campaign deadline" className="rounded-2xl bg-cream px-4 py-3 lg:col-start-5" type="date" value={deadline} onChange={e=>setDeadline(e.target.value)}/></div>{loading?<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{Array.from({length:6}).map((_,i)=><div key={i} className="card h-96 animate-pulse bg-black/5"/>)}</div>:error?<div className="card p-10 text-center"><p className="font-bold text-red-700">{error}</p><button onClick={load} className="btn-primary mt-4"><RefreshCw className="mr-2" size={16}/>Try again</button></div>:items.length===0?<div className="card p-14 text-center"><h2 className="text-2xl font-black">No campaigns found</h2><p className="mt-2 text-black/50">Try removing a filter or searching for a different idea.</p></div>:<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(c=><CampaignCard key={c._id} campaign={c}/>)}</div>}<div className="mt-8 flex items-center justify-center gap-3"><button aria-label="Previous page" disabled={page<=1} onClick={()=>setPage(p=>p-1)} className="rounded-full bg-white p-3 disabled:opacity-30"><ChevronLeft/></button><span className="text-sm font-bold">Page {page} of {Math.max(1,pages)}</span><button aria-label="Next page" disabled={page>=pages} onClick={()=>setPage(p=>p+1)} className="rounded-full bg-white p-3 disabled:opacity-30"><ChevronRight/></button></div></main><Footer/></>}
