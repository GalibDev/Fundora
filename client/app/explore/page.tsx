"use client";
import { useState } from "react";import Navbar from "@/components/navbar";import Footer from "@/components/footer";import CampaignCard from "@/components/campaign-card";import { campaigns } from "@/lib/data";import { Search } from "lucide-react";
export default function Explore(){const [query,setQuery]=useState("");const [category,setCategory]=useState("All");const cats=["All",...Array.from(new Set(campaigns.map(c=>c.category)))];const shown=campaigns.filter(c=>(category==="All"||c.category===category)&&c.title.toLowerCase().includes(query.toLowerCase()));return <><Navbar/><main className="container-app py-14"><p className="eyebrow">Discover your next cause</p><h1 className="mt-3 text-5xl font-black tracking-tight">Ideas looking for believers.</h1><div className="my-9 flex flex-col gap-3 rounded-3xl bg-white p-3 shadow-soft md:flex-row"><label className="flex flex-1 items-center gap-3 px-3"><Search size={19}/><input className="w-full py-3 outline-none" placeholder="Search campaigns..." value={query} onChange={e=>setQuery(e.target.value)}/></label><select className="rounded-2xl bg-cream px-5 py-3 font-bold" value={category} onChange={e=>setCategory(e.target.value)}>{cats.map(c=><option key={c}>{c}</option>)}</select></div><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{shown.map(c=><CampaignCard key={c._id} campaign={c}/>)}</div></main><Footer/></>}
