"use client";
import Script from "next/script";import { useEffect,useRef } from "react";import { useRouter } from "next/navigation";import { toast } from "sonner";import { useAuth } from "@/lib/auth";
declare global{interface Window{google?:{accounts:{id:{initialize:(o:{client_id:string;callback:(r:{credential:string})=>void})=>void;renderButton:(el:HTMLElement,o:object)=>void}}}}}
export default function GoogleSignIn({role="supporter"}:{role?:string}){const ref=useRef<HTMLDivElement>(null);const auth=useAuth();const router=useRouter();const render=()=>{const id=process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID?.trim();if(!id){toast.error("Google Client ID is missing in Vercel");return}if(!window.google||!ref.current)return;ref.current.innerHTML="";window.google.accounts.id.initialize({client_id:id,callback:async({credential})=>{try{await auth.google(credential,role);toast.success("Signed in with Google");router.replace("/dashboard")}catch(e){toast.error(e instanceof Error?e.message:"Google sign-in failed")}}});window.google.accounts.id.renderButton(ref.current,{theme:"outline",size:"large",shape:"pill",text:"continue_with",width:350})};useEffect(render,[role]);return <><Script src="https://accounts.google.com/gsi/client" strategy="afterInteractive" onLoad={render} onError={()=>toast.error("Could not load Google Sign-In")}/><div ref={ref} className="mt-3 flex min-h-11 justify-center"/></>}
