"use client";
import {useState} from "react";import {ImagePlus,Loader2} from "lucide-react";import {api} from "@/lib/api";import {toast} from "sonner";
export default function ImageUpload({value,onChange}:{value:string;onChange:(url:string)=>void}){const [busy,setBusy]=useState(false);const upload=async(file?:File)=>{if(!file)return;if(file.size>5_000_000)return toast.error("Image must be under 5MB");setBusy(true);const reader=new FileReader();reader.onload=async()=>{try{const data=await api<{url:string}>("/uploads",{method:"POST",body:JSON.stringify({image:reader.result})});onChange(data.url);toast.success("Image uploaded")}catch(e){toast.error(e instanceof Error?e.message:"Upload failed")}finally{setBusy(false)}};reader.readAsDataURL(file)};return <label className="flex cursor-pointer items-center gap-3 rounded-2xl border border-dashed border-black/20 bg-cream p-4"><span className="grid size-11 place-items-center rounded-xl bg-white">{busy?<Loader2 className="animate-spin"/>:<ImagePlus/>}</span><span className="text-sm"><b className="block">{value?"Replace image":"Upload campaign image"}</b><span className="text-black/45">JPG, PNG or WebP · max 5MB</span></span><input className="hidden" type="file" accept="image/*" onChange={e=>upload(e.target.files?.[0])}/></label>}
