import Image from "next/image";import Link from "next/link";import { ArrowUpRight } from "lucide-react";import { Campaign } from "@/lib/types";
export default function CampaignCard({campaign}:{campaign:Campaign}){const pct=Math.min(100,Math.round(campaign.raised/campaign.goal*100));return <article className="card group overflow-hidden"><div className="relative h-56 overflow-hidden"><Image src={campaign.image} alt={campaign.title} fill className="object-cover transition duration-500 group-hover:scale-105"/><span className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1 text-xs font-bold backdrop-blur">{campaign.category}</span></div><div className="p-6"><div className="mb-3 flex items-start justify-between gap-3"><h3 className="text-xl font-black leading-tight">{campaign.title}</h3><Link href={`/campaigns/${campaign._id}`} className="grid size-9 shrink-0 place-items-center rounded-full bg-lime"><ArrowUpRight size={18}/></Link></div><p className="mb-5 line-clamp-2 text-sm leading-6 text-black/60">{campaign.story}</p><div className="h-2 overflow-hidden rounded-full bg-black/5"><div className="h-full rounded-full bg-forest" style={{width:`${pct}%`}}/></div><div className="mt-3 flex justify-between text-sm"><strong>{campaign.raised.toLocaleString()} credits</strong><span className="text-black/50">{pct}% funded</span></div></div></article>}
